apiVersion: v1
kind: Service
metadata:
  name: {{ include "dst.fullname" . }}
  labels: {{- include "dst.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector: {{- include "dst.selectorLabels" . | nindent 4 }}
