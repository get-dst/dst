<!-- Anything larger than a bugfix: open an issue first (CONTRIBUTING.md). -->

**What changes, and why**

**How it was verified** <!-- `make ci` green; plus the frontend checks if you touched apps/web -->

- [ ] `make ci` passes locally
- [ ] Commits are signed off (`git commit -s`) — the DCO check enforces this
